# Screenshots

Add one current screenshot or short GIF for each platform:

- `gnome.png` - GNOME Shell top-bar popup
- `linux.png` - Linux tray menu/window
- `windows.png` - Windows tray menu/window
- `macos.png` - macOS menu bar popover
- `site.png` - mosque finder website

Use real captured UI from the current build. Do not use generated mockups for release-facing screenshots.

> **Recapture needed:** the Windows and Linux tray apps moved to the Al Munadi
> charcoal/emerald palette (with a new first-run welcome state, Test/Stop adhan
> and Reset offsets controls), and the GNOME popup highlight changed from blue
> to emerald. Existing screenshots predate these changes.
