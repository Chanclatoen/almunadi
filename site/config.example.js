window.AL_MUNADI_CONFIG = {
    // Full Cloudflare Worker origin used by the mosque finder.
    // Example: 'https://api.almunadi.net'
    // Leave empty only when /api/search is served from the same origin as site/.
    apiBase: '',

    repoUrl: 'https://github.com/Chanclatoen/almunadi',
    releasesUrl: 'https://github.com/Chanclatoen/almunadi/releases',
};
