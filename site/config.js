window.NEXT_PRAYER_CONFIG = {
    // Leave empty when the Worker is mounted on the same host as the site.
    // Otherwise set this to your Worker origin, for example:
    // https://next-prayer-mawaqit-search.your-subdomain.workers.dev
    apiBase: 'https://mawaqitsearch.chanclatoen.nl',
    repoUrl: 'https://github.com/Chanclatoen/next-prayer-mawaqit',
    releasesUrl: 'https://github.com/Chanclatoen/next-prayer-mawaqit/releases',
};
